---
name: sqlitebrowser-build-openssl
description: Build, test, verify, and stage the repository-pinned OpenSSL 3.5.7 on Windows x64 with Visual Studio 2022 and SDK 10.0.22621.0. Use for OpenSSL environment checks, builds, test selection, artifact verification, or staging in this SQLiteBrowser repository; do not use for system-wide OpenSSL installation or non-Windows targets.
---

# SQLiteBrowser OpenSSL Build

This is the Claude Code compatibility entry for the repository's canonical Agent Skill.

Locate the repository root, then read `.agents/skills/sqlitebrowser-build-openssl/SKILL.md` completely and follow it. Resolve every repository-relative path from that root. The canonical instructions and `third_party/openssl/build.cmd` are the single sources of truth; do not copy or reinterpret the build workflow in this compatibility file.
